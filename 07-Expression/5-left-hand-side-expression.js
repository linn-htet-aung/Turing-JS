/**
 * ------------------------------------------------------------------------------------------
 * 1. variables such as i and total
 * ------------------------------------------------------------------------------------------
 */
{
    i = 10;
    total = 0;
}

/**
 * ------------------------------------------------------------------------------------------
 * 2. properties of objects
 * ------------------------------------------------------------------------------------------
 */ 
{
    var obj = {}; // an empty object with no properties
    obj.x = 10; // an assignment expression
}

/**
 * ------------------------------------------------------------------------------------------
 * 3. elements of arrays
 * ------------------------------------------------------------------------------------------
 */ 
{
    array[0] = 20;
    array[1] = 'hello';
}


/**
 * ------------------------------------------------------------------------------------------
 * 4. Invalid left-hand-side errors
 * ------------------------------------------------------------------------------------------
 */
{
    ++(a + 1); 
    // SyntaxError. Attempting to increment or decrement an expression that is not an lvalue will lead to errors.
}
