/**
 * ------------------------------------------------------------------------------------------
 * 1. Value of variable that doesn't been initialized
 * ------------------------------------------------------------------------------------------
 */
{
    let a;
    console.log(a);
}

/**
 * ------------------------------------------------------------------------------------------
 * 2. Value doesn't not exist in Object Properties
 * ------------------------------------------------------------------------------------------
 */
{
    let obj = {
        name: "Alice",
        age: 23,
    }
    console.log(obj.skill);
}

/**
 * ------------------------------------------------------------------------------------------
 * 3. Value doesn't exist in Array Element 
 * ------------------------------------------------------------------------------------------
 */
{
    let arr = [10, 20 , 30, 40];
    console.log(arr[10]);
}
