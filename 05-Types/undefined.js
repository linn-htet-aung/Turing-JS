// Value of variable that doesn't been initialized //
let a;
console.log(a);

// Value doesn't not exist in Object Properties //
let obj = {
    name: "Alice",
    age: 23,
}
console.log(obj.skill);

// Value doesn't exist in Array Element //
let arr = [10, 20 , 30, 40];
console.log(arr[10]);